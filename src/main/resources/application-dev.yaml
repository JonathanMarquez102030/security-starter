spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/dbsecuritytemplate
    username: securityuser
    password: mysecretpassword
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # Mail Configuration for Development
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:jmp19731@gmail.com}
    password: ${MAIL_PASSWORD:kzhb veym sgjf hkzf}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
        debug: false

  sql:
    init:
      mode: always
      schema-locations: classpath:db/security/01-security-schema.sql
      data-locations: classpath:db/security/02-security-data.sql
      continue-on-error: false


# ===========================
# Configuración de OTP en desarrollo
# ===========================
otp:
  length: 6
  expiration-minutes: 5
  max-attempts: 5
  resend-cooldown-seconds: 60

logging:
  level:
    org.springframework.security: INFO
    com.jonathanmarquez.security: DEBUG
    com.jonathanmarquez.security.security.filter: DEBUG
    com.jonathanmarquez.security.security.utils: DEBUG

server:
  port: 8081